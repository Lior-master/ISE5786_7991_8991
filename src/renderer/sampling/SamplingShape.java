package renderer.sampling;

/**
 * Sampling shape for 2D sampling.
 */
public enum SamplingShape {
    /**
     * Circular sampling area, where samples are distributed within a circle of the specified radius.
     */
    CIRCLE,

    /**
     * Rectangular sampling area, where samples are distributed within a rectangle defined by the specified width and height.
     */
    RECTANGLE
}
